# Remaining non-blocking questions

1. Whether the Blender pin should target an exact stable 5.1 build or a development build until 5.1 is fully released.
2. Whether viewer clips should be captured from the desktop session or via Blender-specific export hooks.
3. Whether the broker should eventually move off-Pod once multiple sandboxes are needed.
4. Whether snapshots should remain addon-only or expand to whole-repo overlays for more complex test fixtures.
