#!/usr/bin/env bash
set -euo pipefail

cd /opt/codex/broker
echo "[codex-broker] starting broker service"
python3 - <<'PY'
import fastapi  # noqa: F401
import uvicorn  # noqa: F401
import app.main  # noqa: F401
print("[codex-broker] import preflight ok", flush=True)
PY
exec python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8080
